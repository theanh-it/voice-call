# Cổng chạy server (mặc định 3000)
PORT=3000

# CORS: origin cho phép (dấu * = mọi origin, hoặc https://example.com)
CORS_ORIGIN=*

# Socket.IO: timeout ping (ms)
SOCKET_PING_TIMEOUT=60000
SOCKET_PING_INTERVAL=25000
