# URL backend Socket.IO (để client kết nối).
# Để trống = dùng cùng origin (khi dev qua Vite proxy hoặc build chung backend).
# Ví dụ production: https://api.example.com
VITE_SOCKET_URL=

# Chỉ dùng khi chạy npm run dev:
# Cổng Vite dev server
VITE_DEV_SERVER_PORT=5173
# Proxy /socket.io chuyển đến backend
VITE_DEV_PROXY_TARGET=http://localhost:3000
